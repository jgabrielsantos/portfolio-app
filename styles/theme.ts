import { toRem } from "../src/utils"

export const colors = {
  color1: '#fafafa',
}

export const fonts = {
  fontName: {
    H12: `font-size: ${toRem(12)}; font-weight: bold; font-family: gothammedium;`,
  }
}